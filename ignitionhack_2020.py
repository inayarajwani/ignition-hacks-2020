import textextractor.py

def highlight_text(desired_text):
    right_word = []
    for word in x:
        if desired_text == word:
            right_word.append(word)
        else:
            continue
            
    return right_word        
    print(str(right_word))
    
  



